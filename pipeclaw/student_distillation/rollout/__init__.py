"""Rollout runtime for student distillation.

Import the submodules directly (``rollout.episode``, ``rollout.suite``,
``rollout.runner``, ...).  This package root intentionally exports nothing:
keeping the root free of re-exports means the import surface an engineer must
keep coherent is the submodule layout itself.
"""
